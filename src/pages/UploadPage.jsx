import Button from '../components/common/Button'
import Badge from '../components/common/Badge'
export default function UploadPage({ file, onFileChange, onNavigate }) { const choose = () => onFileChange({ name: '별빛강변_뮤직페스티벌_기본계획(안).hwp', size: '2.4MB', type: 'HWP' }); return <main className="wrap page narrow"><button className="back" onClick={() => onNavigate('dashboard')}>← 분석 문서 목록</button><div className="phead"><div><h1>새 기획안 업로드</h1><p>작성하신 축제 기획안 문서를 업로드하면 AI가 분석에 필요한 항목을 추출합니다.</p></div></div><button className="drop" onClick={choose}><div className="ic">⬆</div><h3>파일을 끌어다 놓거나 클릭해 선택하세요</h3><p>PDF · HWP · HWPX · DOCX 지원 · 최대 50MB<br />기획안 원문 그대로 업로드하시면 됩니다.</p></button>{file && <div className="picked"><div className="filerow"><div className="fi">{file.type}</div><div className="file-info"><div>{file.name}</div><small>{file.size} · 방금 업로드됨</small></div><Badge risk="LOW">업로드 완료</Badge></div><div className="upload-actions"><Button variant="ghost" onClick={() => onFileChange(null)}>취소</Button><Button onClick={() => onNavigate('extract')}>AI Feature 추출 시작 →</Button></div></div>}</main> }
